{"id": "qpp1665702522210", "title": "Configuring the Database Connector", "breadcrumbs": ["DBMS Support", "Database Connectors", "Configuring the Database Connector"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/qpp1665702522210.html", "text": "Configuring the Database Connector You can configure Uniface to access any supported database for the Repository or application data. The default database used for the Repository and application data depends on the operating system. For MS Windows, SQLite is the default database. For Unix and Linux, Sequential is the default database. To configure the database connector and database access, edit the application assignment file. By default, Uniface uses the file dbms.asn to configure database access. Open the file dbms.asn located in UnifaceInstallDir/common/adm . This file is included in several other Uniface assignment files, such as ide.asn and wasv.asn . In the [DRIVER_SETTINGS] section, specify the connector and version, in the format Mnemonic {=} Version . If you omit this setting, or specify the incorrect version, the database connector cannot be used. For example: [DRIVER_SETTINGS] PGS U1.4 Copy where U1.4 specifies the connector version to be used for connecting to PostgreSQL. Optionally, specify parameters for the connector, in the format USYS$ Mnemonic _PARAMS Option { , Option n } For example: [DRIVER_SETTINGS] PGS U1.4 USYS$PGS_PARAMS identifiers = quoted Copy The connector options (parameters) depend on the database and connector. For more information, see the Uniface database connector documentation for your database. In the [PATHS] section of the assignment file, define a path for the connector in the format: $ PathName = Mnemonic : { Database } | { UserName } | { Password } Note: To encrypt a path that includes user name and password, you can use the Pathscrambler utility. For more information, see pathscrambler.exe . You can then use the $PGS path definition in path redirections. For example: [PATHS] $PGS PGS:PostgreSQL35W:public|signor|s3cr!349857 $IDF $PGS ; Path to Repository definitions for entities. $UUU $PGS ; Path to Repository definitions for runtime libraries. $SYS $PGS ; Path to the Repository definitions for print models. $DEF $PGS ; Path to default DBMS database for user data. Copy Related Links Database Connectors To access, retrieve, and store data, Uniface provides database connectors that translate Uniface I/O requests into the target database's data definition and manipulation languages. Path-to-Connector Assignments A path-to-connector assignment describes how to access a specific resource via the appropriate network or database connector. It defines the path name, and assigns it to a configuration that specifies the connector to use, the name and location of the resource, and the credentials required to log on to it. Paths are defined in the [PATHS] section of the application assignment file. [DRIVER_SETTINGS] Use the [DRIVER_SETTINGS] section of the assignment file to specify the database and component engine connectors used, and their parameter options. The options available depend on the connector in use. [PATHS] Use the [PATHS] section of the assignment file to define paths to network and database connectors, which connect to a Uniface Server, DBMS, or to another path. Path assignments can include path-to-connector assignments and path-to-path assignments. Was this topic helpful? Like Dislike"}