{"id": "wnk1665702545934", "title": "UHTTP", "breadcrumbs": ["Uniface Reference", "API Reference", "APIs: Uniface Components", "UHTTP"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/wnk1665702545934.html", "text": "UHTTP The Uniface UHTTP component is an HTTP client that is used for sending and receiving HTTP requests and responses. It provides operations for executing HTTP requests used for web applications and RESTful web services. You can use UHTTP operations to integrate communication with web sites, implement basic browser functionality, retrieve and store data in the cloud, and so on. The component signature is automatically available in Uniface, so you can create a new instance and activate its operations using handles. The UHTTP component's SEND operation enables you to invoke HTTP methods (such as GET , POST , PUT , PATCH , OPTIONS , HEAD , and DELETE ) by submitting an HTTP request to a URL. Other UHTTP operations enable you to prepare content and interpret responses, and to control the behavior of the SEND operation. Note: Although it is possible to use the UHTTP component as an alternative to the SOAP connector, you need expert knowledge to do so. For SOAP communication, we recommend that you use the SOAP connector. Operations The UHTTP component API provides the following operations: SEND —send an HTTP request using the specified HTTP method, such as POST , PUT , or PATCH for file upload and GET for file download. SET_FLAGS —set flags prior to a SEND instruction to determine how Uniface handles HTTP communication. For example, you can specify whether requests can be redirected, determine how the component responds to mismatched or expired certificates for HTTPS, or calculate content length. Note: The SET_PROFILE function is the preferred way to manage HTTP communications and security. SET_PROFILE —set security and HTTP communication options that the UHTTP component uses for sending and receiving requests and responses. This is similar to SET_FLAGS but is based on values in [NET_SETTINGS] which are part of the component’s assignment (.asn) file. SET_TIMEOUT —set the amount of time an HTTP request can take to complete. WRITE_CONTENT —append a segment of data to the UHTTP's content buffer, which will be used by the SEND operation. It cannot be used in combination with the file operations, such as LOAD_FILE_CONTENTS . READ_CONTENT —obtain additional data from the last HTTP request. It cannot be used in combination with the file operations, such as DUMP_FILE_CONTENTS . LOAD_FILE_CONTENTS —load a file, or part of a file, into the request body prior to calling SEND to upload the file. DUMP_FILE_CONTENTS —create a local file by reading a response body containing the file, or a chunk of it. GET_INFO —if required, get information about the request body either before or after the SEND operation. This is most useful when the request body represents the current file, as there is no other way of getting the file length or range. GET_LAST_ERROR —return the last CURL error as an error string. Use it to troubleshoot an error caused by the SEND operation. CLOSE_FILE —close the current file that was being used for chunked file transfer. Configuring UHTTP In the application assignment file, you can use settings and logicals to: Configure a proxy server for HTTP transport. For more information, see [PROXY_SETTINGS] . Configure the default authentication scheme used for SEND requests. For more information, see UHTTP_DEF_AUTH . Set default Content-Type header to be added to POST requests (Windows only). For more information, see UHTTP_WPOST_FORM . Note: The UHTTP component supports HTTPS data transport using libcurl . The certificates must be provided in a ca-bundle.crt file, which is not included in the installation and must be downloaded or created manually and placed in the usys directory. On Windows, if this file is missing, the native Windows certificate store is used automatically. For more information, see HTTPS Protocol . Example: Using UHTTP The following example shows how you can instantiate the UHTTP component and call its operations: Explain this code variables handle vUHTTP numeric vStatus string vURI, vHeaders, vContent, vResponse endvariables newinstance \"UHTTP\", vUHTTP vURI = \"http://mysite.com/op1\" ;Create list of headers putitem/id vHeaders, \"Content-Type\", \"application/xml; charset=UTF-8\" putitem/id vheaders, \"Accept-Charset\", \"ISO-8859-1 ; q = 0.4, UTF-16BE; q=0.9\" ;Assemble content and assign to vContent ... ... ; Call the UHTTP.SEND operation vStatus = vUHTTP->SEND(vURI, \"POST\", \"\", \"\", vHeaders, vContent, vResponse) ... Copy Related Links REST-Compliant Web Services You can use the UHTTP component to create and use REST-compliant web services. Example: Chunked File Upload to OneDrive This example demonstrates how to use the Uniface UHTTP component to upload a file to OneDrive by submitting a request to the OneDrive API. Was this topic helpful? Like Dislike"}