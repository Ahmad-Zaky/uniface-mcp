{"id": "voi1705662348184_2", "title": "Device Translation Tables", "breadcrumbs": ["Platform Support", "Translation Tables", "Device Translation Tables"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/voi1705662348184_2.html", "text": "Device Translation Tables Device translation tables handle output to screens and printers. They map Uniface logical functions onto device functions, making it possible for Uniface to support a wide variety of hardware platforms, including their output devices. Uniface uses a set of standard mnemonics, such as init , bold , clear_screen , formfeed , to represent logical device operations. A device translation table defines the control codes that the device uses to implement a particular operation. For example, a display device such as a VT200 uses the control code sequence ^esc[2J to clear the screen. When you define a device translation table, you map the mnemonic clear_screen to the control code sequence ^esc[2J . Figure 1. Output Code Translation Uniface makes no fundamental distinction between output devices of different types. Both printers and display terminals are defined using the same facility and the same mnemonic codes, but not all possibilities are used for both types of device. For example, a printer definition does not include a code for clear_screen and a terminal often does not have commands for linefeed . For example, when a word should appear in bold, Uniface looks up the control codes associated with the logical function bold in the device table for the current display device. This occurs during the formatting of the data. Since each display device or printer requires a different set of codes, these would be provided in a different device translation tables. Related Links Device Translation Table Syntax A device translation table contains delimiters that mark the beginning and end of the table and sections within the table. Each section contains mapping definitions that define an output code sequence or character, the corresponding Uniface meta code (a function mnemonic or meta character), and optional description or comments. Device Translation Tables for Printing Printer device tables are a collection of sequences (control codes) associated with specific logical functions (such as bold, form feed, and init). The sequences begin with the escape character and are referred to as escape sequences. Translation Tables To mediate between itself and the external environment, Uniface uses translation tables. Translation tables provide input and output mappings between platform-specific device codes and Uniface functions, and between external and internal character sets. Uniface provides translation tables for the platforms it supports, but you can customize these or use them to create your own tables. Defining and Using Translation Tables Uniface uses device translation tables to handle output to screens and printers, and keyboard translation tables to handle input from keyboards. For every system supported, Uniface supplies a set of translation tables in the USYS library. These default tables are sufficient in most cases. Was this topic helpful? Like Dislike"}