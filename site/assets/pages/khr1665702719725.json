{"id": "khr1665702719725", "title": "$ENABLE_CACHE_ALWAYS", "breadcrumbs": ["Uniface Reference", "Configuration Reference", "Configuration Settings: Assignment", "[SETTINGS]", "$ENABLE_CACHE_ALWAYS"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/khr1665702719725.html", "text": "$ENABLE_CACHE_ALWAYS Allow the select cache mechanism when moving to the last occurrence. $ENABLE_CACHE_ALWAYS Defaults Assignment file: application assignment files Section: [SETTINGS] Default value: None Description When $ENABLE_CACHE_ALWAYS is present, each occurrence is retrieved into the select cache when moving to the last occurrence, for example, when using the structure editor function ^LAST_OCC or the ProcScript statement setocc \"*\", -1 . When $ENABLE_CACHE_ALWAYS is not present, the select cache is not used when moving to the last occurrence. This improves performance when information concerning other records is not required. Example: $ENABLE_CACHE_ALWAYS $ENABLE_CACHE_ALWAYS Copy Related Links Retrieval Processing Data is retrieved from the database when the user enters a retrieve profile (that is, a search string) in the desired fields and then enters ^RETRIEVE. Alternatively, ProcScript can be used to assign retrieve profiles to the desired fields in a component. When retrieving data from the database, Uniface selects the required data with the Select connector request, and fetches it into the component with the Fetch connector request. Was this topic helpful? Like Dislike"}