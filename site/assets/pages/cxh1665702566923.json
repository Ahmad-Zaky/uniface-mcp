{"id": "cxh1665702566923", "title": "trigger postSerialize", "breadcrumbs": ["Uniface Reference", "Script Module Reference", "Triggers: System", "trigger postSerialize"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/cxh1665702566923.html", "text": "trigger postSerialize System trigger that processes an occurrence after it is saved from a component into a disconnected record set (XML, JSON, or Struct). Declaration: trigger postSerialize Applies to: Entity Activation: Activated immediately after an occurrence is saved from a component into a disconnected record set. Default behavior: None Behavior upon completion: Setting $status has no effect on trigger processing. Description You can use this trigger to tune the execution of the xmlsave , websave , and componentToStruct ProcScript commands. Related Links Disconnected Record Sets Disconnected record sets provide a way for components to maintain and share data and its state (such as whether the data has been modified) even when a connection to a database is not available. They are typically used for communication between web components (server pages and web services) and are usually implemented as XML, JSON, or Uniface Structs. Uniface XML Stream Processing Uniface uses the xmlsave and xmlload ProcScript statements to transfer and convert data (including validation and modification state) between Uniface component and XML streams. Attributes determine the state of data in an XML stream. These are defined in the DTD for the XML stream. xmlsave Places component data in an XML stream. websave Writes a JSON stream from the data in a component to $webinfo(\"data\"). componentToStruct Writes component data to a Struct. trigger preDeserialize System trigger that processes an occurrence before it is loaded from a disconnected record set (XML, JSON, or Struct) into a component. trigger postDeserialize System trigger that processes an occurrence after it is loaded from a disconnected record set (XML, JSON, or Struct) into a component. trigger preSerialize System trigger that processes an occurrence before it is saved from a component into a disconnected record set (XML, JSON, or Struct). Was this topic helpful? Like Dislike"}