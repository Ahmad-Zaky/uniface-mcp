{"id": "wmj1665702544072", "title": "/ex", "breadcrumbs": ["Uniface Reference", "Configuration Reference", "Configuration Settings: Assignment", "[SERVERS]", "/ex"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/wmj1665702544072.html", "text": "/ex Starts the UST as an exclusive Uniface Server. /ex For example: [PRE_START] TCP:+13002|user|P@55word|server2 Copy [SERVERS] SRV1 = userver.exe asn=srv1.asn /ex Copy Description When used in the [SERVERS] section of the Uniface Router's assignment file, /ex indicates that the specified UST only accepts exclusive server requests. This means that the client must also specify the /ex switch in the [PATHS] section of its assignment file when requesting the specified server. Note: For this reason, it is recommended that you use the/exswitch in the [PATHS] section of the client assignment file, and not in the Uniface Router’s assignment file. For more information, see Effect of /shr and /ex Switches . Once the exclusive server has been started, the client disconnects from the Uniface Router and reconnects to the started server. The Uniface Router no longer routes requests from the client. For more information, see Logical and Physical Connections . If an exclusive Uniface Server is killed or crashes, the recovery behavior depends on whether inheritance was used or whether the server has its own listening port. For more information, see Exclusive Server Failure Recovery . Related Links Uniface Router A multithreaded deployment engine that routes requests of Uniface clients and Uniface servers. Define Uniface Server Types (USTs) Each UST identifies a specific Uniface Server configuration. It can be defined in the assignment file of the Uniface Router, or it can be provided to the Uniface Router by a Uniface Server that has been started manually. /gates Specify a range of ports that the exclusive server will listen on when using a TCP or TLS connection. /hostrc Specify that the host name (rather than the IP address) should be used for reconnecting to an exclusive Uniface Server. /ex Requests an exclusive Uniface Server from the Uniface Router. Was this topic helpful? Like Dislike"}