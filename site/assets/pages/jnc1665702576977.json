{"id": "jnc1665702576977", "title": "$entname", "breadcrumbs": ["Uniface Reference", "ProcScript Reference", "ProcScript: Functions", "$entname"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/jnc1665702576977.html", "text": "$entname Return the name of the current entity or check for the existence of an entity in the component. $entname { ( Entity ) } Parameters Table 1. Parameters Parameter Data Type Description Entity String Name of an entity. If omitted, the current entity is used. Return Values If Entity is not present, $entname returns the name of the current entity (in uppercase). If there is no current entity (that is, if the last node of the active path is not a field or entity), $entname returns an empty string (\"\"). If Entity is present, $entname returns the name of the entity if it is present in the component, otherwise $entname returns an empty string (\"\"). Use Allowed in all component types. Description The $entname function is useful when you are writing global ProcScript, because it allows you to generalize your code. It is also useful to examine this function when you are using the ProcScript debugger to step through ProcScript statements. Example: Inserting a New Occurrence The following example shows a call to a global ProcScript in the entity's create trigger to insert a new occurrence before the first occurrence or to add a new occurrence after the last occurrence: trigger create call AddInsertOcc() if ($rettype = 65) creocc $entname, -1 else creocc $entname, 1 endif end Copy Example: Testing a Condition $entname returns the current entity name, and that may not always be the entity you expect. For example, if ENTITY1 occurs before ENTITY 2 in the component structure, retrieving data for ENTITY2 does not make it the current entity. You can ensure that you are dealing with the correct entity by using setocc . retrieve ENTITY2 if ($entname = \"ENTITY1\") setocc \"ENTITY2\" endif ... Copy Related Links $applname Return the name of the Application Shell. $curocc Return the sequence number of the current occurrence in the hitlist. $fieldname Return the name of the current field or check for the presence of a specified field in the component. $instancename Return the name of the current component instance. $rettype Return the retrieval mode of the outermost entity. Was this topic helpful? Like Dislike"}