{"id": "sse1719818928383", "title": "$curkey", "breadcrumbs": ["Uniface Reference", "ProcScript Reference", "ProcScript: Functions", "$curkey"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/sse1719818928383.html", "text": "$curkey Return the number of the current key in a validateKey or leaveModifiedKey trigger. $curkey Return Values Table 1. Values Returned by $curkey Value Meaning 1 Primary key >1 Key number of a candidate key \"\" An error occurred Table 2. Values returned by $procerror following $curkey Value Error constant Meaning -1304 <UPROCERR_UNKNOWN_CONTEXT> Function not allowed, unknown context. For example, $curkey was encountered in an incorrect trigger. Use Allowed only in a validateKey or leaveModifiedKey trigger. Allowed in all component types, except self-contained Reports. Description The $curkey function returns the key number in the current occurrence for which the validateKey or leaveModifiedKey trigger was activated. Example: Validating Keys and Error Handling The following example uses the $curkey function to perform specific validation for each key and otherwise reports the error and context using $procerror and $procerrorcontext . trigger validateKey selectcase $curkey case 1 ;perform validation for the primary key ... case 2 ;perform validation for candidate key #2 ... case 4 ;perform validation for candidate key #4 ... elsecase message \"Error %%$procerror occurred at %%$procerrorcontext\" message \"Context: %%$dataerrorcontext\" endselectcase end; validateKey Copy Related Links Validate Determines whether the valerr data processing attribute is added for the selected entity node in the DTD tree. This attribute holds the occurrence or field validation error (depending on whether the selected item is an entity or field). Valid values are True or False (default). validatekey Validate a key of the current occurrence. validateocc Validate an occurrence. $keytype Return the type of the specified key. $keyvalidation Identify whether a primary or candidate key requires validation. $totkeys Return the total number of keys for an entity. $dataerrorcontext Return the context of the last validation error. trigger leaveModifiedKey Location for ProcScript that determines what happens after a key (especially a compound key) of an occurrence is modified. Applicable only in form components. trigger validateKey System trigger that defines the procedural checks to validate a modified primary or candidate key. Was this topic helpful? Like Dislike"}