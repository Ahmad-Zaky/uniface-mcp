{"id": "spf1665703007836", "title": "$format", "breadcrumbs": ["Uniface Reference", "ProcScript Reference", "ProcScript: Functions", "$format"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/spf1665703007836.html", "text": "$format Return the field data, formatted according to the display format (DIS). $format $format = FormattedData Return Values Field data, formatted according to the display format (DIS) of the field. Use Allowed in the trigger formatToDisplay and formatFromDisplay or components. Description You can use $format to change formatted data using the FormattedData argument. Use $format only in the field-level formatToDisplay and formatFromDisplay triggers. When the formatToDisplay trigger is activated, $format initially returns the field data, formatted according to the display (DIS) template of the field. If a display template is not defined for the field, the default formats shown in the following table are used: Table 1. Default Display Templates Used by $format Data type Displayed format String As is Numeric { - } nnn { . } Float { - } nnn { . } e nnn Date, Linear Date ccyymmdd Time, Linear Time hhnnsstt Datetime, Linear Datetime ccyymmddhhnnsstt Boolean T or F Image, Raw TRX-coded data When the formatFromDisplay trigger is activated, $format initially returns the raw data for the field, as entered by the user. ProcScript in the formatFromDisplay trigger should convert this raw data into a format acceptable by Uniface. The converted data should be written to $format when the formatFromDisplay trigger completes. In the Debugger, $format can be accessed directly or as variable $103 . Example: Using $format The following example adds a sterling symbol in front of a number if the currency is British: ; trigger: Format if (CURRENCY = \"POUNDSTG\") $format = \"STG %%$format\" endif Copy Related Links trigger formatFromDisplay System trigger that temporarily converts the format of data in a field from an external format into an internal format, and makes that reformatted data available for processing purposes. Drag-and-Drop Formats Drag-and-drop formats are global objects that define drag and drop behavior, including the default action, pop-up menu, and icon that are available when performing drag and drop actions in the tree and picture widgets. They are not used for drag-and-drop widgets. trigger formatToDisplay System trigger that temporarily converts the format of data in a field from an internal format into an external format that is displayed or printed. Was this topic helpful? Like Dislike"}