{"id": "ayv1719391190685", "title": "debug", "breadcrumbs": ["Uniface Reference", "ProcScript Reference", "ProcScript: Statements", "debug"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/ayv1719391190685.html", "text": "debug Start the Uniface Debugger. debug Return Values None Use Allowed in all Uniface component types. Description The debug statement causes the component to enter debug mode. On the client in a GUI environment, it starts the Uniface Debugger, enabling you to enter debugging commands. On a client running in character mode, a debug command line appears at the bottom of the screen. During the development process, it is quite common to place the debug statement in the application-level Switch Keyboard trigger. This statement should be removed once the system goes into production, or embedded in a conditional instruction so that it can only be invoked in specific circumstances. Alternatively, you can use the /deb switch to start the application and the Debugger. For more information, see /deb . Debugging a Uniface Server The debug statement is not implemented for the Uniface Server. The Debugger is neither started nor stopped at a debug statement. This is because a server process on Windows is not able to start a windowing application that needs a desktop to run on. To debug the server application shell, you must start the Debugger before the Uniface Server is started, either manually or by means of the Uniface Router. If you want to run the Debugger on a different machine, you should configure a unique TCP port for the communication. The following example shows the use of the debug statement: ;Exec trigger debug edit end ; end trigger Copy Example: Enabling the Debugger in a Deployed Application To enable the debugger to be started in a deployed application: Add the following ProcScript to the keyboardSwitch trigger of the application shell: trigger keyboardSwitch if ($logical(\"SwitchKeyboard\") = \"debug\") debug endif end Copy If debugging is required for some reason in the deployed application, add SwitchKeyboard=debug to the [LOGICALS] section of the assignment file, and activate the trigger by pressing GOLD Y. Related Links Debugging Uniface Applications Uniface provides tools and ProcScript instructions to help you debug applications during development, and to monitor and control the execution of ProcScript in a running application. Compile Development Objects When you compile development objects, Uniface checks the definition, syntax, and integrity of the object, generates descriptors, and stores the compiled object, either on the file system or in the Repository. nodebug Return focus to the application. For the character mode Debugger, stop the Debugger. trigger keyboardSwitch Interactive trigger that defines the application-wide actions in response to the ^SWITCH_KEY action, such as loading a new keyboard translation table. Debugging in Character Mode On a client running in character mode, the debug ProcScript statement causes a Debug command line to be displayed at the bottom of the screen. Was this topic helpful? Like Dislike"}