{"id": "mel1665702961174", "title": "Upgrade Stored Packages", "breadcrumbs": ["DBMS Support", "Database Connectors", "Oracle", "Oracle Stored Packages", "Upgrade Stored Packages"], "url": "https://docs.rocketsoftware.com/de-DE/bundle/uniface_104/page/mel1665702961174.html", "text": "Upgrade Stored Packages To upgrade packages, use the upgrade packages connector option when you need to: Add packages to tables for which the packages were not yet created. Upgrade all packages to the current major and minor package version of the ORA connector. Update packages to reflect changes in the application model. Note: If you have any doubts about whether all packages are up-to-date or not, it is always good practice to upgrade packages. When performing the upgrade procedure, you do not need to drop the old packages, because Uniface generates the statement: CREATE OR REPLACE PACKAGE ... However, when you change the name of an entity, or remove an entity or change the application model in such a way that an entity no longer has an associated package, the upgrade procedure does not drop the packages that are no longer used. This is harmless, but the old packages waste space in the database. In this case, it is advisable to drop all packages in a schema before performing the upgrade procedure. To upgrade packages: Set the connector option upgrade packages in the USYS$ORA_PARAMS assignment. Make sure that the options disable checks and disable packages are not set. Run the Create Table facility ( /genSql createtable ) for the tables associated with the packages that you want to upgrade. Because the upgrade packages option is set, the Create Table facility only generates the statements required to upgrade the packages, and does not generate statements to create tables and indexes. Execute the generated SQL script using the Oracle utility SQL*Plus or SQL*DBA. It is only possible to create the packages in a schema which already contains the associated tables. If you want to create both the tables and the packages, you should be using the normal functionality of the Create Table facility and not this upgrade procedure. Test your applications with the new packages. Remove the upgrade packages option from the USYS$ORA_PARAMS assignment. At this stage, you may set the disable checks option if you want. Related Links Oracle Stored Packages Uniface generates stored packages containing procedures for most basic I/O operations. upgrade packages Generate packages only with the Create Table utility, not tables and indexes. Was this topic helpful? Like Dislike"}